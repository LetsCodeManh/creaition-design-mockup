import previewOne from "../assets/images/other1.png"

const Preview = () => {
  return <section id="preview">
    <div>
      <img src={previewOne} alt="PreviewPicture" />
    </div>
  </section>;
};

export default Preview;
